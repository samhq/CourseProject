1. Create a python virtual environment (tested with python 3.8.5)
2. Activate the virtual environment and install the required python packages using pip install -r requirements.txt
3. Run python app.py